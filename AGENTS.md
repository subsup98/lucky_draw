# AGENTS.md

이 문서는 이 저장소에서 작업하는 에이전트와 작업자가 따라야 할 운영 규칙입니다.

## 현재 진행 기준

- 현재 신규 목표는 **예약 구매 + 일반 판매 온라인 주문 시스템**입니다.
- 기준 요구사항은 [docs/order-sales-system.md](docs/order-sales-system.md)를 우선 참조합니다.
- 기존 루트 문서(`README.md`, `requirements.md`, `architecture.md`, `api.md`, `policy.md`, `security.md`, `tasks.md`)는 참조 전용입니다. 명시 요청이 없으면 수정하지 않습니다.
- 기존 쿠지 기능과 충돌하는 설계는 즉시 `planning/WORKLOG.md`와 루트 `WORKLOG.md`에 남기고, 전환/병행 여부를 결정한 뒤 구현합니다.

## 역할 폴더

| 폴더 | 역할 | 주요 책임 |
|---|---|---|
| `planning/` | 기획/PM | 요구사항, 우선순위, 정책 결정, 오픈 이슈 정리 |
| `database/` | DB 설계 | Prisma/DB 모델, 마이그레이션, 상태값, 개인정보 저장 구조 |
| `backend/` | 백엔드 | 주문, 결제, 배송, 현장 수령, 알림, 관리자 API |
| `frontend/` | 프론트엔드 | 사용자 화면, 관리자 화면, 상태 표시, 운영 시트 UI |
| `operations/` | 운영/풀필먼트 | 주문 처리, 송장 입력, 순차 발송, 관리자 업무 흐름 |
| `policy/` | 정책/개인정보 | 개인정보 동의, 보관 기간, 환불/배송/입금 정책 |
| `infra/` | 인프라 | 환경변수, 배포, DB/Redis, 모니터링, 백업 |
| `qa/` | QA | 테스트 시나리오, 회귀 테스트, 인수 기준 |

각 역할 폴더는 `AGENTS.md`, `CHECKLIST.md`, `WORKLOG.md`를 유지합니다.

## 작업 절차

1. 작업 전 루트 `AGENTS.md`와 관련 역할 폴더의 `AGENTS.md`를 읽습니다.
2. 루트 [CHECKLIST.md](CHECKLIST.md)에서 전체 우선순위를 확인합니다.
3. 관련 역할 폴더의 `CHECKLIST.md`에서 세부 작업을 확인하고 갱신합니다.
4. 실제 변경이 있으면 해당 폴더 `WORKLOG.md`에 먼저 기록합니다.
5. 의미 있는 마일스톤은 루트 [WORKLOG.md](WORKLOG.md)에 요약합니다.
6. 코드 변경 시 기존 사용자의 미완료 변경을 되돌리지 않습니다.

## 설계 원칙

- 주문, 결제, 배송, 현장 수령 상태는 하나의 필드에 섞지 않고 분리합니다.
- 주문 당시 상품명, 가격, 배송지는 스냅샷으로 보존합니다.
- 예약 구매 순차 발송은 기본적으로 **결제 완료 순**을 권장하되, 정책 확정 전에는 오픈 이슈로 남깁니다.
- 송장 입력과 실제 택배사 인계는 분리 가능한 상태로 설계합니다. MVP에서는 송장 입력 시 `배송 중` 자동 전환을 허용할 수 있습니다.
- 개인정보 필드 접근, 엑셀 다운로드, 주문자 목록 조회는 감사 로그 대상입니다.

## 로그 원칙

- 날짜는 `YYYY-MM-DD` 형식으로 씁니다.
- 형식은 `무엇을 / 왜 / 결과 / 다음 작업` 순서를 권장합니다.
- 막힌 작업은 `BLOCKER:` 접두어를 붙입니다.
- 확정되지 않은 결정은 `OPEN:` 접두어를 붙입니다.
