# frontend / WORKLOG.md

## 2026-07-05 - API 오류 메시지 및 관리자 UI 한국어화

- 무엇을 / 공통 API 클라이언트의 영어 오류 메시지를 한국어 사용자 메시지로 변환하고 관리자 콘솔의 영문/깨진 표시를 한국어로 정리했다.
- 왜 / 한국어 사이트에서 회원가입, 주문, 결제, 관리자 작업 오류가 영어 원문으로 노출되면 사용자가 원인을 이해하기 어렵고 운영 화면 신뢰도가 떨어진다.
- 결과 / `@lucky/api-client`의 `ApiError`가 한국어 메시지를 기본 표시값으로 갖도록 했고, 관리자 로그인/사이드바/브라우저 제목/개인정보 페이지 링크를 한국어로 정리했다. 관리자 로그인 카드는 모바일에서 화면 밖으로 삐져나가지 않도록 반응형 폭으로 수정했고, 관리자 사이드바는 폭과 줄바꿈을 조정했다.
- 다음 작업 / 배포 후 사용자 회원가입 오류, 관리자 로그인/메뉴 화면을 운영 도메인에서 재확인한다.

## 2026-07-05 - 회원가입 생년월일 필수값 누락 수정

- 무엇을 / 사용자 회원가입 화면에서 백엔드 필수 필드인 `birthdate`를 입력받아 전송하도록 수정한다.
- 왜 / 배포 화면 회원가입 시 `birthdate must be a valid ISO 8601 date string` 오류가 발생해 회원가입 풀사이클 테스트가 막혔다.
- 결과 / `/login`과 `/v2/login` 회원가입 폼에 생년월일 날짜 입력을 추가하고 `birthdate`를 가입 API에 전송하도록 수정했다. 비밀번호 입력 검증도 백엔드 최소 10자 정책에 맞췄고 `@lucky/user` 타입체크를 통과했다.
- 다음 작업 / 배포 후 `/v3/login`에서 회원가입, 로그인, 구매 흐름을 다시 확인한다.

## 2026-06-29 - 상품 주문 결제 방식 선택 UI

- 무엇을: 사용자 상품 상세 주문 폼에 `무통장 입금`, `Mock 카드`, `Mock 카카오페이` 선택 UI를 추가했다.
- 왜: 실제 도메인/PG 연동 전에도 카드/간편결제 성공 흐름을 내부 상태 전이 기준으로 테스트해야 하기 때문이다.
- 결과: 무통장은 기존 입금자명 입력 후 주문 접수로 유지하고, Mock 카드/카카오페이는 주문 생성 후 `/payments/intent`와 `/payments/confirm`으로 즉시 결제 완료까지 진행한다. 실제 PG provider가 반환되면 보류 안내 에러를 표시한다.
- 다음 작업: 실제 PG 연동 시 Toss/카카오페이 결제창 진입과 성공/실패 복귀 화면을 운영 도메인 기준으로 연결한다.

## 2026-06-25 - 상품 주문 배송 화면 null 처리 보강

- 무엇을: 관리자 배송 목록/상세 화면에서 `kujiEvent`가 없는 상품 주문도 표시되도록 보강했다.
- 왜: 판매 상품 주문은 쿠지 이벤트가 없으므로 배송 화면이 `kujiEvent.title`을 직접 읽으면 런타임 에러가 발생한다.
- 결과: 쿠지 주문은 쿠지명을, 상품 주문은 주문 품목명을 표시한다. 구버전 응답처럼 `orderItems`가 없더라도 "상품 주문"으로 안전하게 표시한다.
- 다음 작업: 송장 일괄 입력/업로드 UI를 설계한다.

## 2026-06-25 - 예약 구매 순차 발송 관리 UI

- 무엇을: 관리자 상품별 구매자 시트에 예약 구매 입고 수량 기준 대상 선정 UI를 추가한다.
- 왜: 운영자가 별도 시트 계산 없이 결제 완료 순서대로 이번 차수 발송/수령 대상을 확정해야 한다.
- 결과: 예약 상품 선택 시 입고 수량 입력, 대상 미리보기, 대상 선정 버튼을 제공하고 구매자 목록에 `itemStatus`를 표시했다.
- 다음 작업: 송장 일괄 입력/업로드 UI와 배송 지연 기준 확정 후 알림 화면을 보강한다.

## 2026-06-24

- 무엇을: 신규 주문 시스템 기준의 프론트엔드 역할 문서와 체크리스트를 추가했다.
- 왜: 사용자 구매 흐름과 관리자 운영 화면이 분리되어 있고, 특히 주문자 시트/송장/현장 수령 관리는 반복 업무 UX가 중요하다.
- 결과: 사용자 화면, 관리자 화면, UX 점검 항목을 분리했다.
- 다음 작업: 기존 `apps/user`, `apps/admin` 화면에서 재사용 가능한 주문/배송 UI를 확인한다.

## 2026-06-25

- 무엇을: 관리자 상품별 구매자 상황 시트 화면을 추가하고 상품 관리/사이드바에서 진입할 수 있게 연결했다.
- 왜: 백엔드 `GET /admin/orders/products/:productId/buyers` API가 구현되어 있었지만, 운영자가 품목별 구매자·입금·배송·현장 수령 상태를 한 화면에서 확인하는 UI가 필요했다.
- 결과: `apps/admin/app/(admin)/product-buyers/page.tsx` 추가, `상품 관리` 행별 구매자 버튼 추가, 사이드바 `구매자 시트` 메뉴 추가. 관리자 앱 타입체크 통과.
- 다음 작업: 주문 상세 화면에 무통장 입금 확인, 주문 기준 송장 입력, 현장 수령 완료 액션을 보강한다.

## 2026-06-25 — 관리자 주문 상세 운영 액션 보강

- 무엇을: 관리자 주문 상세 화면에서 무통장 입금 확인, 배송/송장 수정, 현장 수령 완료를 직접 처리할 수 있게 했다.
- 왜: 주문 목록에서 상세로 들어온 운영자가 별도 API 호출이나 다른 화면 이동 없이 결제/배송/수령 상태를 마감해야 하기 때문이다.
- 결과: `apps/admin/app/(admin)/orders/[id]/page.tsx`에 상품 주문 품목 표시, 입금 확인 버튼, 배송 수정 모달, 현장 수령 완료 모달을 추가했다.
- 검증: `corepack pnpm --filter @lucky/admin typecheck` 통과.
- 다음 작업: 사용자 상품 목록/상세 및 주문 생성 화면을 연결한다.

## 2026-06-25 — 사용자 상품 목록/상세 및 무통장 주문 화면 구현

- 무엇을: 사용자 앱에 신규 상품 목록, 상품 상세, 무통장 입금 주문 생성 화면을 추가했다.
- 왜: 백엔드 상품/주문 API가 준비되어 있어 사용자가 예약 구매/일반 판매 상품을 직접 선택하고 택배 또는 현장 수령으로 주문할 수 있어야 한다.
- 결과: `apps/user/app/products/page.tsx`, `apps/user/app/products/[id]/page.tsx` 추가. 홈 내비게이션에 상품 링크 추가. 상세 화면에서 수량, 수령 방식, 입금자명, 배송지를 입력하고 `POST /api/sales-orders`로 주문 생성.
- 검증: `corepack pnpm --filter @lucky/user typecheck` 통과.
- 다음 작업: 상품 주문 내역/배송 상태 화면이 신규 `sales-orders` 응답 구조까지 자연스럽게 표시되는지 점검하고 보강한다.

## 2026-06-25 — 사용자 상품 주문 내역/배송 상태 표시 보강

- 무엇을: 마이페이지 주문 목록과 주문 상세 화면이 상품 주문의 품목명, 수령 방식, 배송/현장 수령 상태를 표시하도록 보강했다.
- 왜: 신규 상품 주문은 쿠지 이벤트가 없는 주문이므로 기존 `티켓` 중심 문구만으로는 사용자가 무엇을 주문했는지 알기 어렵다.
- 결과: `apps/user/app/me/page.tsx`, `apps/user/app/orders/[id]/page.tsx`, `apps/user/app/lib/types.ts` 갱신. 상품 품목, 예약 순번, 수령 방식, 현장 수령 상태 표시.
- 검증: `corepack pnpm --filter @lucky/user typecheck` 통과.
- 다음 작업: 카드/간편결제 연동 구조와 알림 이벤트 발행 범위를 점검한다.
## 2026-07-03 사용자 쇼핑몰형 프론트 개편 시작

- 무엇을 / comics-art.co.kr 같은 쇼핑몰형 사용자 홈/상품 목록 UI로 `apps/user`를 개편한다.
- 왜 / 예약 구매와 일반 판매 상품을 사용자가 더 쉽게 탐색하고 테스트할 수 있는 storefront 경험이 필요하다.
- 결과 / `apps/user/app/page.tsx`, `apps/user/app/products/page.tsx`, `apps/user/app/components/Storefront.tsx`를 개편했다. 백엔드 API가 늦거나 꺼져 있어도 2.5초 뒤 데모 상품으로 전환해 테스트 화면이 계속 로딩되지 않도록 했다. `@lucky/user` 타입체크 통과.
- 다음 작업 / 실제 상품 이미지와 카테고리 정책을 확정하고 상세 페이지 상단도 같은 쇼핑몰 톤으로 맞춘다.

## 2026-07-03 v3 storefront 분리 및 API 연동 기준

- 무엇을 / 쇼핑몰형 사용자 화면을 `/v3`, `/v3/products`, `/v3/products/[id]` 라우트로 분리했다.
- 왜 / 기존 루트/기능과 병행하면서 예약 구매 + 일반 판매 주문 시스템의 배포 후보 화면으로 테스트하기 위해서다.
- 결과 / `/v3`와 `/v3/products`는 공개 상품 API(`/api/products`)를 호출하고, API 지연 시 데모 상품으로 렌더링한다. `/v3/products/[id]`는 기존 상품 상세/주문 페이지를 재사용해 `GET /api/products/:idOrSlug`, `POST /api/sales-orders`, 결제 intent/confirm 흐름으로 연결된다. `@lucky/user` 타입체크와 로컬 `/v3`, `/v3/products` 200 응답 확인.
- 다음 작업 / v3 상품 상세를 쇼핑몰 톤으로 재디자인하고, 로그인 필요 주문 흐름/결제 mock/무통장 입금 확인/배송 상태까지 v3 화면에서 회귀 테스트한다.

## 2026-07-03 로컬 API/이미지 경로 확인

- 무엇을 / v3 상품 목록이 실제 백엔드 상품 API와 업로드 이미지 경로를 사용하는지 확인했다.
- 왜 / 배포와 로컬의 차이 때문에 이미지/API 오류가 발생하는지 확인하기 위해서다.
- 결과 / 로컬 DB 포트를 `15432`로 맞춘 뒤 백엔드를 실행했고 `GET /api/products`, `/uploads/4ab5a5f653e36309fc25672d5123de0a.jpg`, Next rewrite 경유 `/api/products`, `/uploads/...`가 모두 200으로 응답했다. 테스트 상품의 `imageUrl`을 기존 업로드 이미지 `/uploads/4ab5a5f653e36309fc25672d5123de0a.jpg`로 연결했다.
- 다음 작업 / 배포 nginx의 루트 redirect(`/v2`)를 v3 전환 정책에 맞게 수정하고 운영 컨테이너 재빌드 후 동일 체크를 반복한다.
## 2026-07-03

- 무엇을 / v3 헤더의 마이페이지/주문조회 보호 링크를 로그인 상태에 따라 분기하도록 수정했다.
- 왜 / 비로그인 사용자가 마이페이지 아이콘을 누르면 `/v3/me`가 먼저 렌더링된 뒤 로그인으로 이동해 중간 화면이 노출됐다.
- 결과 / 헤더 보호 버튼은 access token 확인 후 없으면 refresh를 한 번 시도하고, 실패 시 `/v3/login?next=/v3/me`로 즉시 이동한다. 로그인 성공 후 `next` 경로로 복귀하도록 로그인 페이지도 수정했다. 직접 접근 보호 페이지는 현재 경로 기준 로그인 URL로 리다이렉트한다.
- 다음 작업 / 실제 브라우저에서 비로그인/로그인 세션별 클릭 흐름을 한 번씩 수동 확인한다.

- 무엇을 / v3 storefront 홈과 `/v3/kujis` 목록에 공개 쿠지 API(`/api/kujis`) 연동을 추가했다.
- 왜 / 관리자에서 쿠지를 등록해도 v3가 상품 API(`/api/products`)만 조회해 쿠지가 노출되지 않는 문제가 있었다.
- 결과 / v3 홈에 럭키드로우 카운트와 카드 섹션을 추가했고, `/v3/kujis`, `/v3/kujis/[id]` 라우트를 배포했다. `@lucky/user typecheck`와 운영 `user` 이미지 빌드를 통과했다.
- 다음 작업 / 관리자에서 쿠지를 `ON_SALE` 또는 공개 상태로 전환한 뒤 v3 화면에서 이미지와 상세 이동까지 확인한다.
